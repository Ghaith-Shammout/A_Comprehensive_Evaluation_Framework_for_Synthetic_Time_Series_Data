name: main
channels:
  - defaults
  - conda-forge
dependencies:
  - cudatoolkit=11.3.1=h2bc3f7f_2   # Only keep if you're using CUDA
  - numpy
  - pandas
  - python
  - pytorch
  - scikit-learn
  - scipy
  - sdv
  - matplotlib
  - aeon
  - seaborn
  - cudatoolkit=12.1
